#create a function that takes two arguments(parameters) and returns their sum
def add_numbers(a, b):
    return a + b
num1 = float(input("Enter the first number: "))
num2 = float(input("Enter the second number: "))
print("The sum is:", add_numbers(num1, num2))

# The code above defines a function named add_numbers that takes two arguments a and b.
# The function returns the sum of the two input numbers.
# The user is prompted to enter two numbers, which are stored in the variables num1 and num2.
# The function add_numbers is called with the input numbers, and the result is printed.
# The sum of the two numbers is displayed as output.
