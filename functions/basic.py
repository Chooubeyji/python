#write a function to calculate and return the square of a number
def square(num):
    return num**2

result = square(5)  
print(result)

# The code above defines a function named square that takes a single argument num.
# The function returns the square of the input number.
# The function is called with an argument 5, and the result is stored in the variable result.
# Finally, the result is printed.


